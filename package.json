{
  "name": "tab-collection",
  "version": "2.7.0",
  "private": true,
  "description": "A Chrome/Brave extension for collecting tabs into organized groups",
  "scripts": {
    "test": "node storage.test.js && node tasks/tasks-logic.test.js",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
